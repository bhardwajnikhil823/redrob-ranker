# Redrob ranker - pinned for reproducible Stage-3 runs (CPU only, no network).
numpy==2.3.4
scikit-learn==1.7.2
scipy==1.16.3

# Sandbox demo only (app.py). Not needed to reproduce submission.csv via rank.py.
streamlit>=1.49
pandas>=2.0
